spring:
  application:
    name: config-default
eureka:
  instance:
    hostname: localhost
    ip-address: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    service-uri:
      defaultZone: http://localhost:8761/eureka
  server:
    waitTimeInMsWhenSyncEmpty: 0
